import * as S from './styles'
import { BoxIconCryptoProps } from './types'

const BoxIconCrypto = ({ svgIcon }: BoxIconCryptoProps) => (
  <S.Container>{svgIcon}</S.Container>
)

export default BoxIconCrypto
